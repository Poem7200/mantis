---
name: mantis-base
description: mantis项目 基础规则
---

# 基础编码规范

- 命名约定：类名使用 PascalCase，变量、函数和方法使用 camelCase，文件名和目录名一律使用 kebab-case。
- 文件组织：坚持“一个文件一个导出”，并为公共类和方法编写 JSDoc 注释，方法和类的引用必须在当前文件使用，否则不应保留。
- 函数简洁性：单个函数逻辑应保持单一，长度建议不超过 20 行指令

# TypeScript规范

- 强制类型定义：要求所有变量、函数参数和返回值必须明确声明类型，严禁使用 any。
